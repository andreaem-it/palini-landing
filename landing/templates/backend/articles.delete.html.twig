<div id="deleteModal-{{ item.id }}" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Eliminare Elemento?</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                L'elemento {{ item.title }} verrà eliminato.
            </div>
            <div class="modal-footer">
                <a href="{{ url('admin_articles_delete', {'id' : item.id}) }}" class="btn btn-danger" data-dismiss="modal">Elimina</a>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annulla</button>
            </div>
        </div>
    </div>
</div>